VITE_APP_VERSION=1.0.0

# Presenter / viva (Pass 7)
# VITE_VIVA_CONTROLS=false   # hide Alt+1–5 demo chip on public deploy
# VITE_VIVA_LITE=true        # mid-tier GPU caps (or use: npm run build:viva / .env.viva)
