/**
 * Act II (objective) — scroll-phase timing as fractions of the objective act span.
 * Six beats + overlay choreography.
 */

/** Beat 0 — "We are not replacing the pentester." */
export const OBJ_HUMAN_IN = 0.02;
export const OBJ_HUMAN_FADE = 0.05;
export const OBJ_HUMAN_OUT_START = 0.11;
export const OBJ_HUMAN_OUT = 0.15;

/** Beat 1 — "The craft… stay human." */
export const OBJ_STAY_HUMAN_IN = 0.13;
export const OBJ_STAY_HUMAN_FADE = 0.05;
export const OBJ_STAY_HUMAN_OUT_START = 0.22;
export const OBJ_STAY_HUMAN_OUT = 0.26;

/** Beat 2 — redundant work + loop break overlay */
export const OBJ_REDUNDANT_IN = 0.24;
export const OBJ_REDUNDANT_FADE = 0.05;
export const OBJ_REDUNDANT_OUT_START = 0.36;
export const OBJ_REDUNDANT_OUT = 0.4;
export const OBJ_LOOP_BREAK_AT = 0.3;

/** Beat 3 — "So we built one." (3D orbit converge) */
export const OBJ_UNIFY_IN = 0.38;
export const OBJ_UNIFY_FADE = 0.05;
export const OBJ_UNIFY_OUT_START = 0.5;
export const OBJ_UNIFY_OUT = 0.54;

/** Beat 4 — job-driven one click */
export const OBJ_JOBS_IN = 0.52;
export const OBJ_JOBS_FADE = 0.05;
export const OBJ_JOBS_OUT_START = 0.64;
export const OBJ_JOBS_OUT = 0.68;

/** Beat 5 — pipelines, MITRE, reports */
export const OBJ_APPROACH_IN = 0.66;
export const OBJ_APPROACH_FADE = 0.05;
export const OBJ_APPROACH_OUT_START = 0.88;
export const OBJ_APPROACH_OUT = 0.94;

/** ComparisonBeat row stagger (local fraction within compare window) */
export const OBJ_COMPARE_ROW_STAGGER = 0.018;
export const OBJ_CHIP_STAGGER = 0.025;
