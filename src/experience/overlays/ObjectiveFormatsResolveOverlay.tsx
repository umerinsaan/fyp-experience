/** @deprecated Formats merge is handled by ObjectiveUnifyOverlay on beat 4. */
import type { MotionValue } from 'framer-motion';

export function ObjectiveFormatsResolveOverlay(_props: { progress: MotionValue<number> }) {
  return null;
}
