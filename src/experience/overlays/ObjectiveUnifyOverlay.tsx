/**
 * ObjectiveUnifyOverlay — retired. Beat 4 ("So we built one.") is carried by
 * ObjectiveScene (3D tool logos converging into orbit). No 2D chain or panel.
 */
import type { MotionValue } from 'framer-motion';

export function ObjectiveUnifyOverlay(_props: { progress: MotionValue<number> }) {
  return null;
}

/** @deprecated */
export const ObjectiveSprawlResolveOverlay = ObjectiveUnifyOverlay;

/** @deprecated */
export const ObjectiveFormatsResolveOverlay = ObjectiveUnifyOverlay;
